description: 'Remove abandoned managed worktrees under .agent-worktrees/ when safe'
options:
  -
    name: '--dry-run'
    description: 'Preview cleanup without mutating anything'
  -
    name: '--verbose'
    description: 'Print detailed cleanup steps'
examples:
  - 'php scripts/backlog.php worktree-clean'
  - 'php scripts/backlog.php worktree-clean --dry-run --verbose'
notes:
  - 'Only managed worktrees under `.agent-worktrees/` are auto-cleaned.'
  - 'External worktrees are reported by `worktree-list` and must be cleaned manually.'
