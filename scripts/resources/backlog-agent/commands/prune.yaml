description: 'Remove invalid or orphan entries from agent-sessions.json.'
options:
  - name: '--dry-run'
    description: 'Preview removals without writing changes'
  - name: '--force'
    description: 'Also remove warning entries (worktree gone, process still alive). The process itself is not signalled.'
examples:
  - 'php scripts/backlog-agent.php prune'
  - 'php scripts/backlog-agent.php prune --dry-run'
  - 'php scripts/backlog-agent.php prune --force'
notes:
  - 'Auto-removed: (1) client_pid AND tmux_session both null (launch never finalised), (2) driver isAlive() = false (process gone or tmux session missing), (3) worktree missing on disk AND process not alive.'
  - 'Kept with warning: worktree gone but process still alive. Run `stop --code=<code>` first, then re-run prune; or pass --force to drop the entry without signalling the process.'
  - 'Idempotent: running prune again after convergence is a no-op.'
