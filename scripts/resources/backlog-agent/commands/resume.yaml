description: 'REMOVED — use start --code=<code> instead.'
notes:
  - 'The resume command has been removed. start --code=<code> now handles re-attach (live session), ghost-cleanup (dead session), and new-session creation automatically.'
  - 'Running resume returns an explicit error with a redirect message.'
