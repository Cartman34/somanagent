# === ENVIRONNEMENT ===
.env
.env.local
.env.*.local
!.env.dist
!backend/.env
local/*

# === BACKEND SYMFONY ===
backend/vendor/
backend/var/
backend/config/jwt/
backend/.env.local
backend/.env.*.local

# === SCRIPTS ===
scripts/vendor/
scripts/resources/dependencies.lock
# Portals: relative symlinks to sibling clones, posed locally (see AGENTS.md), not committed.
scripts/toolkit
scripts/backlog

# === FRONTEND REACT ===
frontend/node_modules/
frontend/dist/
frontend/build/
frontend/.env.local

# === SKILLS ===
skills/imported/
!skills/custom/.gitkeep
!skills/imported/.gitkeep

# === DOCKER ===
docker/data/
.docker/

# === IDE ===
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# === LOGS ===
*.log
logs/

# === OS ===
desktop.ini

# === Agents ===
.claude/worktrees/
.claude/settings.local.json
.worktrees/
.agent-worktrees/
.codex
.githooks/

# === Global ===
!.gitkeep

# === Backlog (managed by scripts/backlog/install.php) ===
# Local backlog files — never committed: config.yaml is tuned locally (no override system yet),
# board/review/locks/logs/migrations are per-checkout runtime.
/backlog/config.yaml
/backlog/board.yaml
/backlog/review.md
/backlog/review-result.txt
/backlog/lock
/backlog/log
/backlog/migrations.applied
